info:
  name: Health Check
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3031/
  headers:
    - name: Accept
      value: application/json
    - name: X-Requested-With
      value: XMLHttpRequest
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  Endpoint publico para verificar se a API esta disponivel.

  ## Sucesso
  `200 OK`

  ```json
  {
    "message": "API is Working! "
  }
  ```
